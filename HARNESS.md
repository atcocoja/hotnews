# 项目 Harness 说明

## 什么是 Harness？

Harness 是指确保项目按规则执行的工具、脚本和检查机制的集合。本项目通过以下组件构成一个完整的 harness：

## 核心组件

### 1. 配置层

| 文件 | 作用 |
|---|---|
| `project-config.md` | 项目配置和核心原则 |
| `README.md` | 项目总览 |
| `WORKFLOW.md` | 完整工作流指南 |

**核心原则（最重要）：**
- 选题第一优先级是"模型能力匹配度"
- 视频时长 2-3 分钟（120-180 秒）
- 每天只选一个话题
- 热点来源：微博、抖音、小红书

### 2. 工具层

| 脚本 | 功能 | 使用场景 |
|---|---|---|
| `scripts/tools/topic-evaluator.sh` | 选题评估 | 评估热点话题是否值得制作 |
| `scripts/tools/new-topic.sh` | 创建选题 | 创建新的选题目录和文件模板 |
| `scripts/tools/check-production.sh` | 完整性检查 | 检查生产文件是否完整符合规范 |
| `scripts/tools/topic-queue.sh` | 队列管理 | 管理选题队列状态 |

### 3. 模板层

| 模板 | 用途 | 关键约束 |
|---|---|---|
| `source-log-template.md` | 来源核实 | 必须使用绝对日期 |
| `topic-card-template.md` | 选题决策 | 必须评估模型能力匹配度 |
| `script-template.md` | 视频脚本 | 前3秒必须有钩子 |
| `shot-image-prompts-template.md` | 分镜图提示词 | 禁止精确人物面部 |
| `seedance-prompts-template.md` | Seedance 提示词 | 必须包含分镜声明 |
| `production-checklist-template.md` | 生产检查 | 时长 120-180 秒 |

### 4. 检查层

生产前检查清单（`07-production-checklist.md`）包含 5 层检查：

- **事实层**：日期、数字、争议点
- **脚本层**：钩子、叙事目标、时长、结尾
- **Image 2 层**：一段一图、四宫格、主角一致、无水印
- **Seedance 层**：结构化提示词、素材用途、分镜声明
- **发布层**：标题不误导、封面字可读、标签匹配

## Harness 工作流程

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 选题阶段                                                 │
│    topic-evaluator.sh → 评估话题是否值得制作                │
│    （模型能力匹配度≥60分，总分≥70%）                        │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. 创建阶段                                                 │
│    new-topic.sh → 创建选题目录和文件模板                    │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 填写阶段（按顺序）                                       │
│    00-source-log.md → 来源核实                              │
│    01-topic-card.md → 选题决策（评估模型能力匹配度）        │
│    02-script.md → 视频脚本（前3秒钩子）                     │
│    03-image-prompts.md → 分镜图提示词（禁止精确面部）       │
│    04-seedance-prompts.md → Seedance 提示词（分镜声明）     │
│    05-publish-copy.md → 发布文案                            │
│    06-assets-manifest.md → 参考素材表                       │
│    07-production-checklist.md → 生产前检查                  │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 检查阶段                                                 │
│    check-production.sh → 检查文件完整性                     │
│    （8个必需文件、关键字段、时长、面部风险、日期）          │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 生成阶段                                                 │
│    Image 2 → 生成分镜图（10张2x2四宫格）                    │
│    Seedance 2.0 → 生成视频（10段，I2V模式）                 │
└─────────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. 后期阶段                                                 │
│    剪辑拼接 + 口播配音 → 成品视频                           │
└─────────────────────────────────────────────────────────────┘
```

## Harness 的价值

### 1. 质量保证

- **选题质量**：通过 `topic-evaluator.sh` 确保只做模型能力匹配度高的话题
- **事实准确性**：通过 `00-source-log.md` 强制核实来源和争议点
- **画面安全性**：通过模板和检查清单避免精确人物面部风险

### 2. 效率提升

- **模板复用**：8 个标准化模板，每次只需填写内容
- **自动化检查**：`check-production.sh` 自动检查关键项
- **流程标准化**：`WORKFLOW.md` 明确每一步做什么

### 3. 风险控制

- **合规风险**：避免精确人物面部、政治敏感内容
- **事实风险**：强制标注未核实细节，避免断言未发生的结果
- **技术风险**：Seedance 分镜声明避免四格图被错误动画化

### 4. 知识沉淀

- **经验固化**：每次选题的评估和检查都记录在文件中
- **规则可追溯**：所有规则都在 `project-config.md` 和模板中明确
- **新人友好**：`WORKFLOW.md` 让新成员快速上手

## 如何维护 Harness

### 添加新规则

1. 更新 `project-config.md`（配置层）
2. 更新对应模板（模板层）
3. 更新 `check-production.sh`（检查层）
4. 更新 `WORKFLOW.md`（文档层）

### 添加新工具

1. 创建脚本文件在 `scripts/tools/`
2. 添加可执行权限：`chmod +x scripts/tools/xxx.sh`
3. 在 `WORKFLOW.md` 中添加使用说明
4. 在本文档中添加到工具层表格

### 优化模板

1. 修改 `templates/` 下的对应模板
2. 确保新创建的选题使用新模板（`new-topic.sh` 会自动复制）
3. 通知团队更新现有选题（如需要）

## Harness 指标

### 成功率指标

- 选题通过率：评估话题中≥70分的比例
- 文件完整性：必需文件 100% 存在
- 检查通过率：生产前检查≥80%项勾选

### 质量指标

- 模型能力匹配度：平均分≥60
- 时长准确率：120-180秒范围内的比例
- 面部风险规避率：明确禁止精确面部的比例

### 效率指标

- 选题到完成时间：目标≤24小时
- 检查耗时：目标≤1分钟（自动化）
- 模板复用率：目标≥90%

## 常见问题

### Q: Harness 是否会限制创作自由？

**A: 不会。Harness 的核心原则是"模型能力匹配度优先"，这是技术约束而非创作约束。在满足技术约束的前提下，创作空间是自由的。**

### Q: 是否可以跳过某些检查？

**A: 不建议。每项检查都有其价值：**
- 选题评估：避免浪费在不适合的话题上
- 完整性检查：避免遗漏关键文件或信息
- 生产前检查：避免上线后发现质量问题

### Q: Harness 如何迭代？

**A: 基于实践反馈：**
1. 发现问题（如某类话题经常质量不佳）
2. 分析原因（如评估维度不够）
3. 更新规则（添加新的评估维度）
4. 更新工具（脚本、模板、检查清单）
5. 团队培训（更新文档）

## 总结

本项目的 Harness 由**配置、工具、模板、检查**四层构成，确保：

1. ✅ 只做模型能力匹配度高的选题
2. ✅ 所有文件符合项目标准
3. ✅ 避免常见风险（精确面部、事实错误）
4. ✅ 流程标准化、可复制、可维护

**记住：Harness 不是束缚，而是保障。它让你专注于创作，而不是担心遗漏或犯错。**
