# 热点新闻视频生产工作流

## 快速开始

```bash
# 1. 评估热点话题（交互式）
bash scripts/tools/topic-evaluator.sh

# 2. 创建新选题目录
bash scripts/tools/new-topic.sh N06 topic-slug

# 3. 填写生产文件（按顺序）
# 00-source-log.md → 01-topic-card.md → 02-script.md → 03-image-prompts.md → 04-seedance-prompts.md → 05-publish-copy.md → 06-assets-manifest.md → 07-production-checklist.md

# 4. 检查生产完整性
bash scripts/tools/check-production.sh outputs/N06-topic-slug

# 5. 开始生成素材
# - 用 Image 2 按 03-image-prompts.md 生成分镜图
# - 用 Seedance 2.0 按 04-seedance-prompts.md 生成视频
```

## 核心原则（最重要）

**选题的第一优先级是"模型能力匹配度"**

- 最大化利用 ChatGPT image-2 和 Seedance 2.0 的模型能力
- 每天只选一个最匹配模型能力的热点话题
- 话题来源：微博、抖音、小红书等国内平台
- 视频时长：2-3 分钟（120-180 秒）

## 完整工作流

### 第一步：选题评估

使用 `topic-evaluator.sh` 评估热点话题：

```bash
bash scripts/tools/topic-evaluator.sh
```

评估维度：
1. **模型能力匹配度**（权重40%）：画面生成难度、动作表现需求、风格一致性
2. **人物面部依赖度**（权重30%，反向计分）：能否用背影/侧影/剪影替代
3. **叙事完整性**（权重20%）：起承转合、戏剧性反转/冲突
4. **时长适配性**（权重10%）：2-3分钟能讲清楚

**建议：总分≥70%且模型能力匹配度≥60分才制作**

### 第二步：创建选题目录

```bash
bash scripts/tools/new-topic.sh N06 topic-slug
```

会自动创建：
- `outputs/N06-topic-slug/` 目录
- `images/` 和 `videos/` 子目录
- 8 个必需文件模板

### 第三步：填写生产文件

**按以下顺序填写，每一步都依赖上一步：**

#### 3.1 来源核实（`00-source-log.md`）

- 记录一手来源和二手来源
- 核实事实，标注未核实细节
- 所有日期使用绝对日期（避免"今天"/"昨天"）

#### 3.2 选题决策（`01-topic-card.md`）

- 评估为什么值得做
- 分析模型能力匹配度
- 确定时长（120-180秒）和段数（8-12段）
- 识别画面难点（如精确人物面部）

#### 3.3 视频脚本（`02-script.md`）

- 设计 10 段结构（每段约15秒）
- 前3秒必须有钩子（结果或反差）
- 每段只做一个叙事目标
- 使用绝对日期
- 为每段设计画面任务

#### 3.4 Image 2 分镜图提示词（`03-image-prompts.md`）

- 为每段生成一张 2x2 四宫格分镜图
- 每格对应一个关键帧
- 明确全局视觉约定（风格、色调、禁止项）
- **禁止项必须包括：精确人物面部、大段文字、logo、水印**
- 每格描述包括：景别、主体、动作、场景、氛围

#### 3.5 Seedance 2.0 结构化提示词（`04-seedance-prompts.md`）

- **每段头部必须加【分镜声明】**：说明四格图是分镜参考，不是整体动画
- 使用结构化格式：【风格】【时长】【比例】【氛围】【时间轴】【声音】【参考】
- 时间轴按四格拆解：左上→右上→左下→右下
- I2V 模式：提示词要短，主要依赖首帧分镜图

#### 3.6 发布文案（`05-publish-copy.md`）

- 抖音标题（3个备选）
- 小红书标题（3个备选）
- 短文案（2个版本）
- 标签建议（10个左右）
- 封面字备选（3个）
- 评论区引导

#### 3.7 参考素材表（`06-assets-manifest.md`）

- 列出所有参考素材（图片/视频/音频）
- 注意：Seedance 单条视频最多 9 张图片
- 每个素材必须明确用途

#### 3.8 生产前检查（`07-production-checklist.md`）

- 事实层：日期、数字、争议点
- 脚本层：钩子、叙事目标、时长、结尾
- Image 2 层：一段一图、四宫格、主角一致、无水印
- Seedance 层：结构化提示词、素材用途、分镜声明
- 发布层：标题不误导、封面字可读、标签匹配

### 第四步：完整性检查

```bash
bash scripts/tools/check-production.sh outputs/N06-topic-slug
```

检查项：
- 8 个必需文件是否存在
- 关键字段是否填写
- 时长、段数是否符合标准
- 是否避免精确真人面部
- 日期是否使用绝对日期

### 第五步：生成素材

#### 5.1 用 Image 2 生成分镜图

按 `03-image-prompts.md` 中的提示词，生成 10 张 2x2 四宫格分镜图：

```
images/p01-grid.png
images/p02-grid.png
...
images/p10-grid.png
```

#### 5.2 用 Seedance 2.0 生成视频

按 `04-seedance-prompts.md` 中的结构化提示词，逐段生成视频：

- 每段引用对应的宫格图作为首帧（`@图片1` ~ `@图片10`）
- 按时间轴拆解：左上→右上→左下→右下
- I2V 模式：提示词要短

### 第六步：后期制作

- 剪辑拼接 10 段视频
- 添加口播配音
- 添加字幕（可选）
- 导出成品

## 常见问题

### Q1: 如何判断一个话题是否适合做？

**A: 使用 `topic-evaluator.sh` 评估。核心是"模型能力匹配度"：**

- ✅ 适合：体育赛事（台球、足球等）、自然现象、建筑、抽象概念
- ⚠️ 谨慎：需要精确人物面部、复杂文字内容、特定品牌logo
- ❌ 不适合：政治敏感人物、需要精确面部识别、版权图像

### Q2: 为什么必须避免精确人物面部？

**A: AI 生成精确真人面部有两大风险：**

1. 法律风险：可能侵犯肖像权、名誉权
2. 技术限制：AI 生成的人物面部与真实人物有明显差异，影响可信度

**替代方案：**
- 使用背影、侧影、剪影
- 使用手部特写、球杆特写
- 使用远景、俯拍
- 使用风格化处理（不写实）

### Q3: Seedance 2.0 的"分镜声明"是什么？

**A: 分镜声明是为了防止 Seedance 将四格图作为整体动画化：**

```
【分镜声明】@图片1 是四格分镜脚本参考图（Storyboard），不是要被整体动画化的单张画面。
请将其理解为四个独立镜头的视觉参考，按照分镜一（左上格）→ 分镜二（右上格）→ 分镜三（左下格）→ 分镜四（右下格）的顺序，
依次生成四段连续镜头，严禁将四格图作为整体进行动画处理。
```

不加分镜声明可能导致：
- 四格图被当作一张整体图片动画化
- 画面混乱，不符合预期
- 浪费 token 和时间

### Q4: 时长为什么是 2-3 分钟？

**A: 基于三个原因：**

1. **平台特性**：抖音、小红书用户注意力有限，2-3 分钟是最佳时长
2. **模型能力**：image-2 和 Seedance 2.0 生成的视频质量在 2-3 分钟内最稳定
3. **叙事完整性**：热点话题通常 2-3 分钟能讲清楚，太短讲不透，太长容易拖沓

### Q5: 如何提高视频质量？

**A: 三个关键点：**

1. **选题是关键**：模型能力匹配度高的选题，质量天然更好
2. **分镜图要准**：image-2 生成的分镜图质量直接影响 Seedance 的视频质量
3. **提示词要准**：结构化提示词 + 分镜声明 + I2V 短提示词

## 工具参考

| 脚本 | 用途 |
|---|---|
| `scripts/tools/new-topic.sh` | 创建新选题目录 |
| `scripts/tools/topic-evaluator.sh` | 评估热点话题 |
| `scripts/tools/check-production.sh` | 检查生产文件完整性 |
| `scripts/tools/topic-queue.sh` | 管理选题队列 |

## 配置参考

| 文件 | 内容 |
|---|---|
| `project-config.md` | 项目配置和核心原则 |
| `README.md` | 项目总览 |
| `WORKFLOW.md` | 本文件，工作流指南 |
